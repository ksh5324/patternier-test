{
  "name": "patternier-test-fsd-js",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "pnpm exec patternier check",
    "check:json": "pnpm exec patternier check --format json",
    "check:sarif": "pnpm exec patternier check --format sarif",
    "summary": "pnpm exec patternier check --summary"
  }
}
